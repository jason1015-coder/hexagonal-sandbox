
#first commit


# First, remove the incorrect remote
git remote remove origin

# Add the correct remote URL
git remote add origin https://github.com/jason1015-coder?Inhexit.git

# Rename your branch from master to main
git branch -m master main

# Push to GitHub
git push -u origin main



#update 



# Stage your changes
git add .

# Commit with a message
git commit -m "game base update"

# Push to GitHub
git push origin main --force
